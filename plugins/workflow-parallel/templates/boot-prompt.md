@{{plan-path}} @TODO.md — 너는 {{TICKET-KEY}} 전담 worktree 세션. 다른 {{N}}개 worktree 와 병렬 진행 중. 브랜치 {{BRANCH}} 이미 체크아웃 (origin/{{MAIN}} 분기). 의존: {{DEPS}}. 진행: (1) mcp__atlassian__getJiraIssue 로 {{TICKET-KEY}} fetch (cloudId {{CLOUDID}}) — Description / 작업 범위 / 테스트 전략 / 완료 조건 확인. (2) plan §{{SECTION}} + TODO.md 와 교차확인. (3) 구현 (CLAUDE.md 컨벤션, 한국어 OK). (4) pnpm install 은 메인 세션에서 완료, pnpm build && pnpm test (ticket 본문의 테스트 전략대로). (5) commit (기존 톤 git log --oneline -10). (6) git push -u origin {{BRANCH}}. (7) gh pr create (기존 톤 gh pr list --merged --limit 5). 막히면 stop 후 보고, 아니면 자율 진행.
